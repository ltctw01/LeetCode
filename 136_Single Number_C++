class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int result = nums[0];
        int i = 1;
        for(i = 1; i < nums.size(); i ++){
            result ^= nums[i];
            
        }
        return result;
    }
};
